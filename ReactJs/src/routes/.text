{
  /* <Routes>
  <Route
    index
    element={<Index />}
  />

  <Route
    path="login"
    element={<Login />}
  />

  <Route
    path="/dashboard/profil"
    element={<Profil />}
  />

  <Route
    path="*"
    element={<NotFound />}
  />

  <Route
    path="user/*"
    element={<UserRoute />}
  />

  <Route
    path="perusahaan/*"
    element={<CompanyRoute />}
  />

  <Route
    path="produk/*"
    element={<ProductRoute />}
  />

  <Route
    path="invoice/*"
    element={<InvoiceRoute />}
  />
</Routes> */
}


isLogin.login && path != "/login" ? (
                element
              ) : (
                <Navigate to={"/login"} />
              )